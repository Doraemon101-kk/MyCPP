#include "text_analysis.hpp"
#include <cassert>
#include <iostream>

int main()
{
    // TODO: 空输入、大小写、标点、重复、停用词、同频、K=0、K超长。
    // 调用自己实现的接口，逐项写实际断言，而不是只打印预期。
    // 完成后删除下方提示，并仅在断言通过时返回0。
    std::cerr << "Tests are not implemented; this is not a passing result.\n";
    return 2;
}
